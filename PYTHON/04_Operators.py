# OPERATORS -

a=10
b=3

# Python divides the operators in the following groups:

# Arithmetic operators   + - * / % ** //

print("a+b : ", a+b)    
print("a-b : ", a-b)    
print("a*b : ", a*b)    
print("a/b : ", a/b)    
print("a%b : ", a%b)    
print("a**b : ", a**b)    
print("a//b : ", a//b)  # gives floor value of quotient

# ---------------------------------------

# Assignment operators   = += -= *= /= %= //= **=  ( x+=3 same as x = x+3)

# ---------------------------------------

# Comparison operators   == != > < >= <= 

# ---------------------------------------

# Logical operators      and or not

# ---------------------------------------

# Identity operators     is  is not

# ---------------------------------------

# Membership operators   in  not in

# ---------------------------------------

# Bitwise operators      
# Operates on binary numbers, simple logic operations

"""
    ~ 	NOT	- Inverts all the bits
    & 	AND	- Sets each bit to 1 if both bits are 1
    |	OR	- Sets each bit to 1 if one of two bits is 1
    ^	XOR	- Sets each bit to 1 if only one of two bits is 1 
    <<	left shift  - Shift left by pushing zeros in from the right
    >>	right shift	- Shift right by pushing copies of the leftmost

"""