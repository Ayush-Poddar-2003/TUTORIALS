"""

HISTORY -
    -> Python is a popular programming language 
    -> It was created by Guido van Rossum, and released in 1991.
    -> It was designed in c language.

Why Python?
    -> works on different platforms (Windows, Mac, Linux, Raspberry Pi, etc).
    -> has a simple syntax
    -> has syntax with fewer lines 
    -> runs on an interpreter system, code can be executed as soon as it is written.
    
The most recent major version of Python is Python 3

-------------------------------------------------------------------
Python Indentation

    -> Indentation refers to the spaces at the beginning of a code line.
    -> In other programming languages, Indentation in code is for readability only,
       Python uses indentation to indicate a block of code.
    -> Number of spaces is up to programmer but it has to be at least 1, genrally 4.

"""




"""
More on Print statement
The syntax of a print statement looks something like this:

print(object(s), sep=separator, end=end, file=file, flush=flush)
 
Other Parameters of Print Statement
object(s): Any object, and as many as you like. Will be converted to string before printed
sep='separator': Specify how to separate the objects, if there is more than one. Default is ' '
end='end': Specify what to print at the end. Default is '\n' (line feed)
file: An object with a write method. Default is sys.stdout
Parameters 2 to 4 are optional
"""